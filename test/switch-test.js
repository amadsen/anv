// switching to the latest version results in the current node version
// being the latest published version

// switching to an older version results in the current node version 
// being the older version
